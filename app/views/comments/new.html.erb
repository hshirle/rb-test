<h1>New Comment</h1>

<%= form_with scope: :comment, url: comments_path, local: true do |form| %>

  <p>
  
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>

    <%= form.submit params[:user_id] %>
  </p>
<% end %>
